# 项目交接文档

> **2026-07-15 攻坚进展**：以下原文档记录的核心差距已基本解决，整体完成度约 **90%**。
>
> - **四阶段形体已改为目标序列**：人物 → 3/4 右向人物（程序化侧面剪影，额-鼻-下巴轮廓一眼可辨，辫子独立收细垂于脑后）→ 微翻开立体书（书脊/双封面/7 层页扇/前口断面）→ 单环土星（倾角 68°，环从球体前后穿越，338/338 前后遮挡点，shader 按遮挡压暗，无卫星）。
> - **转场已对齐 Dala 机制**：方向性分批延迟（STAGGER_SPREAD=5 + qinticInOut，秩=目标阶段扫掠轴排序）、每粒子弹簧刚度抖动 ±1e-4、乘法爆散（峰值 ~5×，移动端 ×0.6）、转场期整云平移+旋转（净零回正）；跨阶段粒子用 48 分带匈牙利匹配（0→1 按辫/身/晕分组保拓扑，位移均值 0.215）。
> - **交互补齐**：点击触发外扩行波涟漪（1.4s 指数衰减，弹簧自然回稳，不阻塞滚动、与悬停解耦，已调至"清晰不过度"）；悬停半径/抖动随指针速度增强。
> - **光效**：颜色增益 1.3、z 展开 3.6、深度 alpha 衰减、前景视差增强；bloom/bokeh/vignette 维持 Dala 参数。
> - **验收状态**：`npm run check` 全绿（57 tests + lint + typecheck + build）；`python3 scripts/verify-particle-targets.py` PASS（形体/匹配/契约全项）；无头 Chrome 在 390/768/1109/1440 四宽度 × 四阶段共 16 面板零横向溢出、零控制台错误；转场中间态与涟漪截图目检通过。
> - **尚余**：真机 iPhone/Android 性能验证；用户亲自滚动体验的主观手感确认；确认验收后按下方"验收底线"移除旧 Dala bundle、旧 CSS 与 `?runtime=dala` 回退入口（当前仍保留）。
> - 注：Stage 1 侧面像为程序化剪影（`PROFILE_BUST_OUTLINE`），非 2.5D 旋转——旋转方案经两轮迭代确认不可读后按预案回退；移动端竖屏下形体按既有构图缩小，可辨识度以桌面为准。
>
> **2026-07-15 第二轮视觉精修（用户反馈驱动）**：
> - **第二页人物 = 第一页镜像**：按用户要求改为"同一个人只换方向"——`generate_profile_points` 现在直接把 stage0 粒子水平镜像（x→1−x），完全同一批粒子/辫子/深度，转场变成干净的水平翻转。校验器 `validate_profile` 相应改为断言镜像关系（x 反相关 ≤−0.98，y/z 漂移 ≤0.01）。
> - **粒子形体整体放大**：`STAGE_PLACEMENTS` 缩放 About 1.74→2.02、Project 1.74→1.9、Publication(书) 1.12→1.66、Contact(土星) 0.96→1.5，并内收 placement.x 防止裁边。
> - **悬停手感复刻 Dala**：去掉会挖出中心空洞的径向推开，改为 Dala 式局部激荡——光标附近粒子沿各自随机轴振动并放大（`HOVER_SCALE` 0.75→0.95、`HOVER_WORLD_SCALE` 0.52→0.9 扩大影响范围），呈现"膨胀、更松散、无空洞"、与提亮同步的效果。
> - **首页/各页布局**：hero 名字/正文/职称字号加大、`--hero-name-size`/`--act-title-size` 上调、hero 列加宽；Project/Publication 桌面改为右侧内容列**垂直居中**（去掉 sticky），与左侧粒子人物之间留出清晰间距（Publication 因列表长仍顶部锚定）；Contact 联系文案标题加大、行星安全区 26rem→32rem，配合放大的土星填充左右空间。
> - 验收：`npm run check` 全绿（57 tests + lint + typecheck + build）；无头 Chrome 在 390/768/1109/1440 四宽度、四阶段（真实滚动）零横向溢出、零控制台错误；镜像翻转、放大、悬停激荡、各页布局均截图确认。

## 目标

自主实现接近 Dala 的单画布粒子体验：GPU 粒子模拟、鼠标交互、滚动驱动的连续变形、后期光效及配套 CSS，不依赖旧 Dala 运行时。四阶段应为：

1. **About**：粒子人物。
2. **Project**：由第一页人物丝滑变形为**头朝右侧的人物**。
3. **Publication**：变形为轮廓清晰、易识别的**书籍**。
4. **Contact**：变形为带**一圈且仅一圈**独立土星环的粒子星球。

## 当前完成度与差距

整体约完成 **55%（架构可用，视觉对标尚未完成）**。

- **已具备**：独立 Three.js/WebGL2 运行时、浮点纹理 GPU 模拟、单 Canvas、滚动进度映射、指针跟随、Bloom/Bokeh/Vignette/噪点、响应式降级、运行时生命周期和二进制粒子目标。
- **核心目标错误**：当前实际阶段仍是“人物 → 灯泡 → 地球/球体 → 双环并带卫星的土星”，必须改为“人物 → 右向人物 → 书籍 → 单环土星”。
- **转场不足**：目前主要是目标点线性混合加扰动，缺少 Dala 式的分层散开、惯性拖尾、阶段节奏、形体连续性和稳定落位，因此不够丝滑，也不像同一批粒子自然重组。
- **交互不足**：只有局部悬停影响；点击没有明确、可控且会自然衰减的粒子反馈，与目标交互质感差距明显。
- **形体可读性不足**：人物朝向、辫子分离和尖端收细仍需精修；书籍需具备书脊、封面、页层与厚度；土星只能有一圈清楚分离的倾斜环，不能出现双环或卫星干扰。
- **光效与材质不足**：虽已接入后期处理，但粒子层次、发光阈值、景深焦点、近远层速度、色彩响应和暗部细节尚未达到 Dala 的空间感。
- **CSS/滚动协同不足**：内容排版已可用，但粒子构图、文本留白、阶段停留区间和滚动转场节拍仍未形成统一导演效果。

## 主要挑战

1. **目标点对应关系**：不能只生成四组点；需要为同一粒子建立跨阶段的稳定匹配与分批次变形顺序，避免整体“融化再拼装”。
2. **人物到人物的连续性**：第二页应尽量复用头部、颈部和辫子的拓扑关系，只完成朝向与姿态变化，而非重新随机采样。
3. **书籍识别度**：粒子数量有限时，必须优先保留外轮廓、书脊、页面分层和透视厚度。
4. **单环土星遮挡**：环需要正确的前后遮挡、倾角和粒子密度，才能表现“一整圈穿过球体”，而不是两条平行带。
5. **Dala 质感对标**：差距不只在模型形状，还在模拟参数、粒子几何、景深、光晕、镜头视差、转场时间曲线与滚动节奏的整体配合。
6. **性能与稳定性**：10,000 粒子、双缓冲模拟和多重后期效果需兼顾桌面流畅度、移动端显存及 WebGL 上下文恢复。

## 建议实施顺序

1. 重做四阶段目标生成器和验证器，先锁定“人物 / 右向人物 / 书籍 / 单环土星”。
2. 增加跨阶段粒子匹配、分层转场曲线、速度继承与落位阻尼，重点打磨前两次转场。
3. 补充点击冲击/涟漪反馈，并与悬停排斥、滚动状态解耦。
4. 对照 Dala 逐项调校粒子尺寸、前景粒子、镜头视差、Bloom、景深、暗角和噪点。
5. 最后统一 CSS 构图与滚动节奏，在 390/768/1109/1440 宽度完成浏览器截图和控制台验收。

## 验收底线

- 四阶段形体一眼可辨，顺序和朝向完全正确。
- Contact 只有一个完整、倾斜、与球体有前后遮挡关系的环。
- 滚动中无突跳、无明显随机重排；人物到右向人物的连续感最强。
- 悬停和点击反馈清晰但不过度，停止交互后自然回稳。
- 视觉层次明显接近 Dala，文字始终清晰。
- 目标分辨率无横向溢出，控制台无错误，`npm run check` 与粒子目标验证全部通过。
- 验收通过后，才移除旧 Dala bundle、原 CSS、旧点阵素材及临时回退入口。
